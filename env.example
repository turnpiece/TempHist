# Environment variables for TempHist
# Copy this file to .env and adjust values as needed

# API Base URL (optional - will auto-detect based on domain if not set)
# VITE_API_BASE=https://api.temphist.com

# For local development, leave VITE_API_BASE unset to use localhost:3000
# For dev deployment, leave VITE_API_BASE unset to use api.temphist.com (same as production)
# For production, leave VITE_API_BASE unset to use api.temphist.com

# Note: Both dev.temphist.com and temphist.com use the same production API server
